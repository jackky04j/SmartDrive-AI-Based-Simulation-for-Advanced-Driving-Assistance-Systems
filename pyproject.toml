[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "smartdrive-adas-simulation"
version = "0.1.0"
description = "Two-phase computer-vision and CARLA ADAS simulation project"
readme = "README.md"
requires-python = ">=3.10,<3.13"

[tool.pytest.ini_options]
testpaths = ["tests"]
